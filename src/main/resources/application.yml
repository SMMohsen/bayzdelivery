spring:
  datasource:
    url: ${DB_URL}
    username: db_user
    password: 123qwe
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

server:
  port: 8081
  servlet:
    context-path: /api

bayzdelivery:
  deliver:
    minutes-allowed: 45
